# Optional: only needed when Codex built-in image_gen is unavailable
# and the user explicitly chooses API-backed guide image generation.
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_IMAGE_MODEL=gpt-image-2
OPENAI_IMAGE_SIZE=2048x1152
OPENAI_IMAGE_QUALITY=high

# Optional: deep browser inspection password variable.
WEBSITE_HEALTH_ADMIN_PASSWORD=
